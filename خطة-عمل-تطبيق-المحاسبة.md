# خطة عمل: تطبيق محاسبة سطح مكتب (فواتير / عروض أسعار / سندات قبض / محاضر استلام)

> إعداد: مهندس برمجيات (خبرة 20+ عاماً) — الهدف: تصميم عملي، بسيط، قابل للتوسّع، وسريع التنفيذ (MVP خلال أسابيع وليس أشهر).

---

## 1. الرؤية والنطاق

تطبيق سطح مكتب (Windows بشكل أساسي، مع إمكانية macOS/Linux لاحقاً) يسمح للمستخدم بـ:

- إنشاء مستندات من عدة أنواع: **فاتورة، عرض سعر، سند قبض، محضر استلام، وعنوان مخصص آخر** يضيفه المستخدم بنفسه.
- طباعة المستند على **ورقة A4 بدون هوامش (Full Bleed)** فوق خلفية "ورقة الشركة" (الهيدر/الفوتر/الشعار) التي يتم اختيارها كقالب من الإعدادات.
- إدارة كاملة محلياً (Offline-first)، بدون الحاجة لإنترنت أو سيرفر خارجي.

**مبدأ التصميم الأساسي:** الفاتورة = طبقة بيانات شفافة (HTML/CSS) تُطبع فوق طبقة خلفية (صورة/PDF لورقة الشركة). هذا يبسّط كل شيء لاحقاً (التصميم، الطباعة، التصدير).

---

## 2. اختيار التقنية (Stack)

| الطبقة | الاختيار | السبب |
|---|---|---|
| إطار التطبيق | **Electron** (أو Tauri كبديل أخف) | نفس تقنيات الويب (HTML/CSS/JS) تجعل تصميم الفاتورة والطباعة والتموضع فوق صورة الخلفية أمراً بسيطاً جداً؛ نظام الطباعة في Chromium ممتاز ويدعم `@page { size: A4; margin: 0 }` مباشرة |
| الواجهة | **React + TypeScript** | مكوّنات قابلة لإعادة الاستخدام (سطر منتج، حقل اختياري..) وسهولة الصيانة |
| التنسيق | **Tailwind CSS** | يسرّع بناء نموذج الفاتورة والإعدادات |
| قاعدة البيانات | **SQLite** (عبر `better-sqlite3` أو `Drizzle ORM`) | ✅ الأنسب هنا — انظر القسم التالي |
| توليد PDF | **Chromium Print to PDF** (مدمج في Electron) أو `Puppeteer` عند الحاجة لتصدير آلي | لا حاجة لمكتبة PDF خارجية معقّدة؛ نفس الصفحة المعروضة تُطبع كما هي |
| التعبئة والتوزيع | **electron-builder** | ملف تثبيت `.exe` بسيط + تحديثات تلقائية لاحقاً |

> **لماذا Electron وليس .NET/WPF أو Tauri؟**
> - Electron أسرع تطويراً وأسهل صيانة لفريق صغير أو مطوّر واحد، خاصة أن جوهر المشكلة هنا هو "تخطيط صفحة وطباعة" وهذا مجال الويب ممتاز فيه.
> - Tauri أخف بكثير في الحجم والاستهلاك (Rust backend)، ويصلح كخطوة تحسين لاحقة (v2) بعد استقرار المنتج، لأن الواجهة (React) يمكن نقلها شبه كاملة.

---

## 3. قاعدة البيانات: **SQLite** — الاختيار الأمثل

**لماذا SQLite تحديداً؟**

1. **بدون سيرفر** — ملف واحد (`app.db`) محلي، يعمل فوراً بدون تثبيت أو إعداد.
2. **موثوقة وقياسية** — تُستخدم في مليارات الأجهزة (Chrome, Android, iOS)، ACID كامل، لا فقدان بيانات.
3. **قابلة للتوسع فعلياً** — تتحمل ملايين السجلات براحة لتطبيق محاسبة فردي/شركة صغيرة-متوسطة، والأداء يبقى ممتازاً مع فهرسة بسيطة (Indexes).
4. **سهولة النسخ الاحتياطي** — نسخ ملف واحد = نسخة احتياطية كاملة؛ سهل إرساله بالبريد أو رفعه لـ Google Drive/OneDrive.
5. **مسار ترقية واضح مستقبلاً** — إذا احتجت لاحقاً عمل متعدد المستخدمين/سحابي، يمكن الانتقال إلى **PostgreSQL** بنفس مخطط الجداول تقريباً (خصوصاً إذا استخدمت ORM مثل Drizzle/Prisma الذي يدعم الاثنين).

**الخلاصة:** SQLite الآن، مع تصميم المخطط (Schema) بحيث يكون "جاهزاً للهجرة" (استخدام UUID كمفاتيح أساسية بدلاً من Auto-increment، تجنّب ميزات SQLite الخاصة جداً).

---

## 4. تصميم قاعدة البيانات (Schema)

```sql
-- أنواع المستندات (قابلة للتوسعة من المستخدم)
CREATE TABLE document_types (
  id TEXT PRIMARY KEY,          -- 'invoice' | 'quote' | 'receipt' | 'delivery_note' | 'custom-xxxx'
  name TEXT NOT NULL,           -- الاسم الظاهر: "فاتورة"، "عرض سعر"، أو عنوان مخصص
  is_system INTEGER DEFAULT 0,  -- 1 للأنواع الأساسية، 0 للأنواع التي أضافها المستخدم
  default_template_id TEXT,     -- القالب الافتراضي لهذا النوع
  sort_order INTEGER DEFAULT 0
);

-- قوالب ورقة الشركة (الخلفيات)
CREATE TABLE templates (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  background_path TEXT NOT NULL,   -- مسار صورة/PDF ورقة الشركة
  page_size TEXT DEFAULT 'A4',
  stamp_x REAL,                    -- إحداثيات الختم (نسبة % من الصفحة كي تبقى متناسبة)
  stamp_y REAL,
  stamp_width REAL,
  created_at TEXT DEFAULT (datetime('now'))
);

-- المستندات (الفاتورة / عرض السعر / السند / المحضر...)
CREATE TABLE documents (
  id TEXT PRIMARY KEY,               -- UUID
  document_type_id TEXT NOT NULL REFERENCES document_types(id),
  number TEXT NOT NULL,              -- رقم تسلسلي لكل نوع (يُدار في جدول counters)
  doc_date TEXT NOT NULL,            -- التاريخ فوق الجدول
  client_label TEXT DEFAULT 'السادة',-- قابل للتخصيص لكل مستند أو كإعداد افتراضي
  client_name TEXT NOT NULL,
  note_title TEXT,                   -- عنوان الملاحظة (اختياري)
  note_body TEXT,                    -- نص الملاحظة (اختياري)
  footer_note TEXT,                  -- ملاحظة سفلية (اختياري)
  template_id TEXT REFERENCES templates(id),
  show_tax_column INTEGER DEFAULT 0, -- تُحسب تلقائياً وتُخزَّن هنا وقت الحفظ
  show_discount_column INTEGER DEFAULT 0,
  subtotal REAL DEFAULT 0,
  tax_total REAL DEFAULT 0,
  discount_total REAL DEFAULT 0,
  grand_total REAL DEFAULT 0,
  status TEXT DEFAULT 'draft',       -- draft / final / cancelled
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
);

-- بنود المستند (جدول المنتجات)
CREATE TABLE document_items (
  id TEXT PRIMARY KEY,
  document_id TEXT NOT NULL REFERENCES documents(id) ON DELETE CASCADE,
  sort_order INTEGER DEFAULT 0,
  item_name TEXT NOT NULL,      -- البند
  description TEXT,             -- الوصف
  quantity REAL NOT NULL DEFAULT 1,
  unit_price REAL NOT NULL DEFAULT 0,
  tax_percent REAL,             -- NULL = غير مستخدم لهذا السطر
  discount_value REAL,          -- NULL = غير مستخدم لهذا السطر
  line_total REAL NOT NULL
);

-- حقول مخصّصة إضافية فوق الجدول (تحقيق "سهولة إضافة حقل هنا")
CREATE TABLE document_custom_fields (
  id TEXT PRIMARY KEY,
  document_id TEXT NOT NULL REFERENCES documents(id) ON DELETE CASCADE,
  field_label TEXT NOT NULL,
  field_value TEXT,
  sort_order INTEGER DEFAULT 0
);

-- عدّاد الترقيم التسلسلي لكل نوع مستند
CREATE TABLE counters (
  document_type_id TEXT PRIMARY KEY REFERENCES document_types(id),
  prefix TEXT DEFAULT '',
  next_number INTEGER DEFAULT 1
);

-- إعدادات عامة للتطبيق (شركة، عملة، لغة، القالب الافتراضي...)
CREATE TABLE settings (
  key TEXT PRIMARY KEY,
  value TEXT
);

-- فهرسة أساسية للأداء
CREATE INDEX idx_documents_type_date ON documents(document_type_id, doc_date);
CREATE INDEX idx_items_document ON document_items(document_id);
```

**ملاحظات على المخطط:**
- عمود `client_label` يحقق طلب "السادة أو كلمة أخرى قابلة للتخصيص" — يُخزَّن كنص حر لكل مستند، مع قيمة افتراضية في `settings`.
- `document_custom_fields` يحقق "سهولة إضافة حقل هنا" دون الحاجة لتعديل بنية الجدول لكل حقل جديد.
- `tax_percent` و `discount_value` بقيمة `NULL` (وليس صفر) هو المفتاح لتحقيق: **"البنود الاختيارية لا تظهر بعد الحفظ إذا كانت فارغة"** — منطق العرض (Render) يتحقق: هل يوجد أي سطر فيه قيمة غير NULL لهذا العمود؟ إن لم يوجد، يُخفى العمود بالكامل من جدول الطباعة.

---

## 5. منطق "الأعمدة الاختيارية" (نقطة دقيقة تستحق تركيزاً هندسياً)

عند الحفظ والطباعة، التطبيق يطبّق هذه القاعدة تلقائياً:

```
show_tax_column      = عمود الضريبة يظهر  ⇔  يوجد بند واحد على الأقل بقيمة tax_percent ≠ NULL
show_discount_column = عمود الخصم يظهر    ⇔  يوجد بند واحد على الأقل بقيمة discount_value ≠ NULL
```

نفس المنطق يُطبَّق على:
- **الملاحظة**: تظهر فقط إذا كان `note_body` غير فارغ (وعندها فقط يظهر `note_title` معها).
- **الملاحظة السفلية**: نفس الشيء.
- **الحقول المخصّصة**: كل حقل فارغ القيمة لا يُطبع.

هذا يعني أن جدول المنتجات نفسه **مرن العرض**: قد يكون بعمودين إضافيين، أو بدونهما، حسب البيانات الفعلية — دون أي إعداد يدوي من المستخدم وقت الطباعة.

---

## 6. نظام القوالب والطباعة (A4 بدون هوامش)

1. من **الإعدادات ← القوالب**: يرفع المستخدم صورة/PDF لورقة الشركة (بالحجم A4: 210×297 مم) ويسمّيها.
2. عرض المستند في التطبيق = طبقتان (Layers) متراكبتان بنفس أبعاد A4:
   - **الطبقة الخلفية**: صورة القالب (`background-image`, تملأ الصفحة بالكامل `100% 100%`).
   - **الطبقة الأمامية**: بيانات المستند (جدول، تاريخ، اسم عميل...) بخلفية شفافة تماماً.
3. إعداد CSS للطباعة الفعلي:
   ```css
   @page {
     size: A4;
     margin: 0;
   }
   body { margin: 0; }
   .page {
     width: 210mm;
     height: 297mm;
     position: relative;
   }
   .page-background {
     position: absolute; inset: 0;
     width: 100%; height: 100%;
     object-fit: cover;
   }
   .page-content {
     position: absolute; inset: 0;
     /* هوامش داخلية للمحتوى فقط، فوق الخلفية الكاملة */
   }
   ```
4. **الختم**: عنصر (شعار/صورة PNG شفافة) موضعه `position: absolute` بإحداثيات `stamp_x`, `stamp_y` (كنسبة % من عرض/ارتفاع الصفحة كي يبقى ثابت التناسب على أي طابعة). يقوم المستخدم بسحبه (Drag) في شاشة إعداد القالب لتحديد مكانه فوق أي منطقة من التصميم، ويُحفظ الموضع مع القالب.
5. **التصدير**: نفس الصفحة المعروضة تُصدَّر كـ PDF جاهز للطباعة أو الإرسال (عبر Electron `webContents.printToPDF`)، وتُطبع مباشرة عبر أي طابعة متصلة.

---

## 7. تجربة المستخدم (UX) — الشاشات الأساسية

| الشاشة | الوظيفة |
|---|---|
| **لوحة التحكم** | قائمة كل المستندات (فلترة حسب النوع/التاريخ/العميل)، أزرار إنشاء سريع |
| **إنشاء/تعديل مستند** | اختيار النوع (فاتورة/عرض سعر/سند قبض/محضر استلام/مخصص) → نموذج ديناميكي: التاريخ، تسمية العميل + الاسم، جدول البنود (إضافة سطر بضغطة واحدة `+`)، إظهار/إخفاء الضريبة والخصم لكل سطر بزر صغير، ملاحظة اختيارية، حقل مخصص اختياري (`+ إضافة حقل`) |
| **المعاينة/الطباعة** | عرض حي (Live Preview) بنفس شكل الطباعة النهائي فوق القالب المختار |
| **الإعدادات** | بيانات الشركة، القوالب (رفع/تعديل/تحديد موضع الختم)، تسمية العميل الافتراضية، أنواع المستندات المخصّصة، الترقيم التسلسلي لكل نوع، العملة واللغة |

**مبدأ تصميم مهم:** النموذج (Form) يُبنى من نفس تعريف نوع المستند — أي أن إضافة "عنوان مخصص آخر" (نوع مستند جديد) لا يتطلب كتابة كود جديد، بل فقط إضافة سطر في `document_types` واستخدام نفس محرر البنود العام.

---

## 8. معمارية الكود (مختصر)

```
/src
  /main            → Electron main process (نافذة، طباعة، ملفات، نسخ احتياطي)
  /renderer        → React app
    /features
      /documents    → إنشاء/تعديل/عرض المستندات
      /templates    → إدارة القوالب وموضع الختم
      /settings     → إعدادات عامة
    /components     → مكوّنات مشتركة (سطر بند، حقل مخصص، DatePicker...)
    /db             → طبقة الوصول لقاعدة البيانات (repositories) + migrations
  /shared           → أنواع TypeScript المشتركة بين main و renderer
```

- **Migrations**: استخدام أداة migration بسيطة (مثل `drizzle-kit` أو ملفات SQL مرقّمة يدوياً) منذ اليوم الأول، لأن مخطط قاعدة البيانات سيتطوّر.
- **نسخ احتياطي تلقائي**: نسخة من `app.db` عند كل إغلاق للتطبيق (أو يومياً) في مجلد منفصل، مع الاحتفاظ بآخر N نسخ.

---

## 9. خطة التنفيذ (مراحل عملية)

### المرحلة 0 — الأساس (أسبوع 1)
- إعداد مشروع Electron + React + TypeScript + Tailwind + SQLite.
- تصميم وتنفيذ Schema الأساسي + Migrations.

### المرحلة 1 — الإعدادات والقوالب (أسبوع 2)
- شاشة رفع القالب (صورة ورقة الشركة).
- محرر موضع الختم (Drag & Drop فوق معاينة A4).
- إعدادات بيانات الشركة، تسمية العميل الافتراضية.

### المرحلة 2 — إنشاء المستندات (أسبوع 3–4)
- نموذج المستند العام (تاريخ، عميل، ملاحظة، جدول بنود ديناميكي).
- منطق إظهار/إخفاء الأعمدة الاختيارية تلقائياً.
- الحقول المخصّصة.
- أنواع المستندات الأربعة الجاهزة + آلية إضافة نوع مخصص.

### المرحلة 3 — الطباعة والتصدير (أسبوع 5)
- تركيب الطبقتين (خلفية + محتوى) بأبعاد A4 بدون هوامش.
- طباعة مباشرة + تصدير PDF.
- اختبار على طابعات مختلفة (بعض الطابعات تفرض هامشاً افتراضياً — يجب تعطيله من إعدادات الطباعة).

### المرحلة 4 — التلميع والاستقرار (أسبوع 6)
- الترقيم التسلسلي، النسخ الاحتياطي، البحث والفلترة في لوحة التحكم.
- اختبار شامل + تعبئة (Packaging) كملف `.exe`.

> يمكن ضغط الجدول الزمني إلى 3–4 أسابيع بمطوّر واحد متفرّغ إذا تم تبسيط المرحلة 0 (تجاهل الميزات غير الأساسية أولاً وإضافتها تدريجياً).

---

## 10. مخاطر ونقاط يجب الانتباه لها

- **الطباعة بدون هوامش تختلف بين الطابعات** — يُنصح بإضافة خيار "معايرة" بسيط في الإعدادات (إزاحة X/Y بالمليمتر) لتعويض فروقات الطابعة الفيزيائية عن التصميم الرقمي.
- **دقة صورة القالب** — يجب اشتراط دقة لا تقل عن 300 DPI لورقة A4 (≈ 2480×3508 بكسل) لتفادي التشويش عند الطباعة.
- **الترقيم التسلسلي** — يجب أن يكون آمناً من التكرار عند التراجع عن مستند محفوظ (استخدام معاملة/Transaction عند الحفظ + الترقيم).
- **النسخ الاحتياطي** هو أهم عنصر أمان في تطبيق محاسبة محلي بالكامل — لا تعتمد على المستخدم لتذكّره يدوياً.

---

## 11. ملخص القرارات الهندسية الرئيسية

| القرار | الاختيار |
|---|---|
| إطار العمل | Electron + React + TypeScript |
| قاعدة البيانات | SQLite (ملف واحد محلي، قابل للترقية لـ PostgreSQL لاحقاً) |
| الطباعة | HTML/CSS طبقتان فوق A4 بدون هوامش عبر Chromium |
| إضافة أنواع مستندات جديدة | بيانات (Data-driven) لا كود جديد |
| الحقول الاختيارية | تُخزَّن كـ NULL وتُخفى تلقائياً عند الطباعة إن لم تُستخدم |
| موضع الختم | إحداثيات نسبية % محفوظة لكل قالب، بمحرر سحب وإفلات |
